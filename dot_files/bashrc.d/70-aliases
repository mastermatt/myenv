#!/usr/bin/env bash

alias git=hub
alias d=docker
alias dc=docker-compose
alias v=vagrant

alias ls='ls -lAGh'
alias pr='git publish && git spr'

alias npmreinstall='rm -f package-lock.json && rm -rf node_modules && NR_NATIVE_METRICS_NO_BUILD=true npm install'
